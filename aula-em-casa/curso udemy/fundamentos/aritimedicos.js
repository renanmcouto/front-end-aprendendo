const [a, b, c ,d] = [5, 4, 15, 3]

const soma = a + b + c + d
const subtracao = a - d
const multiplicacao = c * b
const divisao = c / a
const modulo = b % d 

console.log(soma, subtracao, multiplicacao, -divisao, modulo)